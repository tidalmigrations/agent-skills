# Demo samples

Source: https://guides.tidal.cloud/modernization-assessment.html

Zip: https://s3.ca-central-1.amazonaws.com/tidal.assets.public/demo-data/demo-data.zip

| Folder | Role |
| --- | --- |
| `equinox-project/` | Default / fast; Windows-timely |
| `nop-commerce/` | Fuller; use WSL on Windows hosts |
| `demo-records/` | Spreadsheet import (servers / DBs / apps) — optional estate context |
| `sct-results/` | Sample SCT uploads — optional; not required for analyze-code flow |

Equinox GitHub shortcut: `https://github.com/EduardoPires/EquinoxProject` (`git clone --depth 1`).

Prefer equinox for short live demos; use nop-commerce when a richer eCommerce sample is needed.
